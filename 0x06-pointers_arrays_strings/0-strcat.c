#include <string.h>
/**
 * *_strcat - concatenates two strings
 * @dest: destination for concatenated string
 * @src: src string to be concatenated to
 * Return: the concatenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

