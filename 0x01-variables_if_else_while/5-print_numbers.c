#include <stdio.h>
/**
 * main - execution starts from here
 * Return: 0 ends the program
 * prints single digit number of base 10
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
